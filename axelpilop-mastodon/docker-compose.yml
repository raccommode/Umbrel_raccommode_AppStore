version: "3.7"

services:

  app_proxy:
    environment:
      # The format here is: <app-id>_<docker-service-name>_1
      APP_HOST: sparkles-web_1
      APP_PORT: 3000
    
  web:
    image: tootsuite/mastodon
    user: "1000:1000"
    init: true
